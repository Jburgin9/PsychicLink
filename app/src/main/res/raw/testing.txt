Store this information in a db instead of in classes
do the calculations

There is a game
-game count

The game is round(s)
-win||loss

The game has a user
-win/loss ratio

In a round a user has 1 chance to guess before the game being over

If the user guesses right

Display the users guessing result and the percent of their guesses over time